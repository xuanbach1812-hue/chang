{
  "name": "chang-landing",
  "version": "1.0.0",
  "description": "Landing page - Dinh Thi Thu Chang | Visa & Work Permit, Korean Import Distribution, Vocational Training",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=chang-landing"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.0.0"
  }
}
